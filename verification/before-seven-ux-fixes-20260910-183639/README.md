# Orbit — current standalone copy

The six requested revisions and follow-up artwork/motion corrections are installed on Samsung SM-S938B (10 September 2026). Browser, native and signed-build checks passed; current artwork and particle motion were inspected on the phone. The installed APK matches `dist/Orbit.apk` (SHA256 `bf8824404f675373c67bdacaada669d723ede4414260321b6c43f169bf0d6526`). See `HANDOFF.md` and `verification/art-quality-motion-result.json` for current evidence and limits; `verification/six-fixes-result.json` preserves the earlier physical sweep. The third owned Strength verification session is open while the user reviews focus. The reconnect heartbeat stays paused and `C:/HA/HEALTH APP` is unchanged.

## Current behaviour

- Home retains its orb, metric swipes, period taps and folding cards. Content continues behind the frosted bottom bar, with clearance for the final card. The bar stays at the bottom when its launcher opens. The launcher now shares the cards' charcoal tones: flat icons, one-line Body / Workouts / Health labels, and no raised icon bubbles.
- Pages and controls use short fades and small slides. Page masks, page copies and automatic expanding-shell transitions are removed. Direct card/orb gestures and the playful countdown retain their own interactions.
- Hero readings use dot matrix in the workout timer, countdown and Body composition. The active workout's walking graphic is replaced by a 72-dot pattern that changes shape on tap. Decorative slogans are removed across the pages. Countdown dots respond to touch. Motion stays enabled regardless of the system motion preference, as explicitly requested; manual orb pause and hidden/offscreen cleanup remain.
- Tap the timer to enter focus: its numbers use the touch-responsive countdown particles without an orbit. Full-width album art starts at the top edge and fades behind the smaller clock and controls; extra timer labels are hidden. The player appears only for an actual track, including a paused track, and appears automatically when music starts. No-track, permission and error responses hide the entire player. Missing artwork keeps real metadata and supported controls. The music-app launch control stays a plain icon. Workout Pause/Finish remain independent; timed workouts retain elapsed/remaining selection.
- Health contains Body, Workouts and a compact expandable Oxygen strip. The Home metric duplicates are removed. Oxygen offers seven date-labelled readings instead of a wrapped number list.
- Body has a tappable composition ring, compact metric choices and an exact-date chart. Its graph is a smooth **2D line with soft shading**; the rejected raised-strip treatment is removed. The treatment remains scoped to Body.
- Tap the Sleep summary to open a night timeline. Each interval has a start, end and duration; the timeline crosses midnight correctly. Selecting a stage highlights all matching intervals and a subtle interval outline identifies the exact interval. The dotted cursor and dotted grid lines are removed. Non-editable app text no longer selects or opens the Android copy/translate menu; editable inputs retain normal text editing. Stage totals, previous/next intervals and previous/next nights remain accessible.
- The selected bar's top dot is removed throughout Home. Reading tooltips and range controls remain.
- Workouts still support Walking, Running, Cycling and Strength, open/time targets, countdown cancellation, pause/resume/finish and saved history. Android notifications share the confirmed stored state and request Live Update promotion. The notification uses neutral artwork and organised duration, target and available distance. Pace is excluded. The target remains a fixed duration so it cannot count below zero. Notification Pause and direct Resume were physically checked.

## Data boundary

Health measurements are labelled demo data ending 8 September 2025. The new sleep chronology is an explicit demonstration fixture that preserves each displayed total; real stage times cannot be recovered from aggregates. Workout sessions started in the app are actual records. Optional outdoor tracking uses Android phone GPS with precise-location permission for distance, speed, pace, elevation and a local route plot. No map service or location upload is used. Watch tracking and the native Health data store remain separate. Completed workouts open grouped timing, movement and energy details; older time-only records remain readable. Optional weight supplies a labelled energy estimate; demo body weight is never used. Estimate: MET × kg × 3.5 / 200 × active minutes, using the 2024 Adult Compendium defaults (Walking 3.5, Running 7.5, easy Cycling 4, Strength 3.5). It includes resting energy during active time and is not a measured physiological value. [Adult Compendium](https://pacompendium.com/)

Music uses Android's active media sessions. **Music access** in workout setup opens Android's notification-access settings. The user has already enabled access on the checked Galaxy; actual BitChord titles, artwork and track changes rendered during the phone sweep. The listener ignores notification contents; Orbit reads media metadata and sends controls without storing music history or downloading artwork. Bitmap and accessible local content artwork are supported; remote-only artwork is omitted. Artwork loads on a worker thread, using the best shared source up to2048px/JPEG94 within a bounded bridge payload, without enlarging small originals. The page keeps the previous decoded cover until the new image can crossfade. The cover stays anchored while controls enter/exit, and pause/resume, reading changes and foreground return preserve timer/player nodes. Agent verification did not change the user's music playback. Transport commands were checked against the isolated browser bridge; broad music-app compatibility remains for real use. Queue preloading and immediate native-to-page music updates are proposed after the user's delay report, but are not in this build.

## Verification and recovery

`node verification/check.cjs` passes. Rendered checks in `verification/motion-review.html` pass at 390 and 320 pixels, including sleep interval selection, full-stage highlighting, countdown/timer controls, saved state, navigation, layout and simple transitions. The native bridge and recorded-workout browser flow are covered. `verification/native-check.ps1` checks compiled altitude validation, resume tokens and geographic distance. Physical verification is tracked separately in the handoff.

Build with `python android/build.py`. Scripts, CSS, icons and the font are bundled offline; the APK signature is verified. Keep `android/signing/orbit-local.p12` for compatible updates. The build preserves its previous APK.

The focused player has a separate rendered check in `verification/focus-music.html`, covering decoded1600px artwork, intermediate crossfade opacity, page-edge anchoring, layer cleanup, focus exit/reversal, metadata escaping, play/pause/skip/seek, stale scrubs, native errors, hidden empty states, automatic track discovery, setup permission access, timer continuity and disposal. Use an isolated origin with no active workout; the test restores saved state. Test artwork is confined to that verification page. Artwork bounds and aspect preservation are included in `verification/native-check.ps1`; an actual phone source's pixel dimensions were not measured.

Previous installed-build evidence and hashes: `verification/focus-music-launcher-result.json`. Current six-fix checks and limitations are tracked in `HANDOFF.md`. Earlier hero/sleep evidence remains in `verification/hero-sleep-frost-result.json`. Recoverable baselines are `verification/before-six-fixes-20260910-162812/` and `verification/before-fullbleed-music-20260910-174442/`, including hash manifests. Older recovery points are retained. These contain sources being changed and the previous APK, not dependency trees.

The earlier short phone sweep verified the full-width player, notification duration after its target, notification Pause/Resume, saved timing and pauses, preserved previous records, sleep long-press suppression and timeline scrubbing without the dotted cursor. Two time-only Strength verification records remain in history (17:34, active09:13;17:51, active01:42). The later18:23 Strength verification session remains open during the user's review. Its real artwork and animated clock are recorded in `verification/six-fixes-phone/quality-motion.mp4`; that clip starts after focus entry and does not prove entry/exit or track crossfade timing. Those transitions passed the rendered browser checks. Real outdoor GPS accuracy and Watch/native Health integration are not qualified by these checks.
