# Orbit — standalone Steps copy

Current motion revision: Today / 7D / 30D uses one sliding violet pill; chart markers and the reading tooltip also travel between selections. The four darker charcoal-frost cards now change height and position continuously, reveal their contents after the shell opens, and hide contents before folding back. Orb translation and contraction preserve its existing SVG and all 646 particles. Position and velocity survive reversals, using the exact critically damped spring equation inspected in Kaito's `contactDynamics.ts`; finger tracking moves the card/orb layout together. Scrolled content returns progressively during collapse. Reduced motion snaps directly to the destination.

All 602 frames of the supplied 60 fps reference were extracted and visually examined in sequence. The complete phase log is `verification/motion-reference/REVIEW.md`; all frames, 31 sequential contact sheets and a frame manifest are retained there. `verification/motion-review.html` records actual browser geometry every animation frame and displays captured intermediate DOM/SVG states. At 390 and 320 pixels, opening and repeated reversals preserved orb/particle identity and returned to the initial geometry, with at least 10.28 pixels between the orb bounds and period selector and no horizontal overflow. These instrumented recordings are motion evidence, not a device performance benchmark.

The existing runnable check passes data coverage plus spring timing, position/velocity continuity, delayed reveal, animation completion, scroll return and reduced-motion endpoints. Browser checks exercised period taps and keyboard changes, upward/downward card drag, chart selection and expanded scrolling. Recoverable baseline: `verification/before-continuous-motion/`. This revision is HTML only; no new Android build or phone installation.

The complete HTML palette now follows the original Signal preview: `#0a0a0c` charcoal, neutral silver text and borders, and `#b69cff` violet selections and data accents. This includes the page, frame, controls, live bar, charts, dialogs, inputs and fallback card backgrounds; the frosted material and layout remain. Browser review covered the main view and options dialog, and the existing functional checks pass. Recoverable baseline: `verification/before-signal-palette/`. HTML only; no new phone installation.

Current HTML revision: all four curved cards have translucent charcoal frost, silver edges, bounded backdrop blur and opaque fallbacks. Expanded cards have a small gap so their individual surfaces remain visible. The orb's swipe hint/dots and the three decorative phrases are removed. For Intake → 30D, the orb and live bar show daily average calories; the nutrition summary also uses daily averages. Today and 7D retain their existing totals. The default demonstration period averages 2,083 kcal/day. Browser checks covered the 30D result, narrow 320/390 layouts, opening and scrolling through the frosted cards; the existing runnable check verifies the average across all 30 selectable dates and preserves other periods. Screen-reader announcements now update when the period changes. The baseline is `verification/before-frosted-cards/`. This is HTML only.

The orb now reuses Signal's actual SVG projection, regular 646-point silver grid, fine violet ring, dotted number typography, slow idle rotation and direct swipe rotation. It is embedded in this HTML, with four-metric selection and the existing pause control preserved. Browser checks covered a sideways drag to Heart rate, keyboard selection of Sleep, a tap to Intake, and the expanded panel layout; 320- and 390-pixel layouts were inspected. The runnable check also covers gesture suppression, the four-metric cycle and pause/reduced-motion/hidden/offscreen animation stops. The previous HTML and check are backed up in `verification/before-signal-orb/`. This update is HTML only; the phone installation is unchanged.

Visual revision after the rejected shelf-like stack: the orb now uses the available upper space; the four panels sit immediately below the period control with 12-pixel overlaps, restrained edges and one readable overview label. The front panel is compact, and its bar sits directly below it. Expanded panels preserve their contents and scrolling. The rejected version and screenshot are retained in `verification/rejected-shelf-stack/`. The Sleep view and open/closed layout were inspected in the browser, and the existing runnable checks pass. This remains an HTML-only revision.

The current HTML has the new live-bar and panel-stack interaction. Its four existing panels begin folded together; swipe/drag upward or tap the bar to expand, and downward to collapse. The bar follows the selected orb metric and date/period. Expanded panels retain their charts, tables and normal scrolling; dragging downward inside the content collapses only when that content is already at the top. The bar also supports Up/Down keys, with hidden panel controls removed from keyboard focus. This HTML revision has **not** been rebuilt or installed on the phone; the installed Orbit APK is the earlier four-metric version.

Stack verification: real browser tap, upward/downward drag, keyboard expand/collapse and focus, expanded-panel scrolling to the comparison panel, and metric-dependent bar updates passed. The layout was inspected at 320 and 390 pixel widths. The runnable check covers gesture direction/threshold/scroll guards, open/closed state, all four live summaries, and the earlier metric/data checks. Touch input handlers and reduced-motion styling are included; this new stack has not been physically tested on a phone or integrated with Samsung's system Now bar. The original full-page HTML baseline is in `verification/before-panel-stack/index.html`.

Open `index.html` in a modern browser. The font, styles, graphics and demonstration data are embedded, so the HTML works offline. Keep `Manrope-OFL.txt` with redistributed copies. The separate Android version is `dist/Orbit.apk`, installed on the connected Galaxy S25 Ultra on 9 September 2026 as `com.mani.orbit`. It has its own icon, app storage and goal settings; the original Health app and Signal preview are unchanged.

Swipe the orb left or right to move between Steps, Heart rate, Sleep and Intake. Tap or keyboard arrows also change metrics. The figures, summaries and charts follow the chosen metric. Today, 7D and 30D change the period; the date button explores 30 demonstration days ending Monday, 8 September 2025, matching the reference. Additional earlier sample records support complete previous-period comparisons. Charts support keyboard selection and the arrow buttons open reading tables. Options contains the daily step goal and rotation toggle; the system reduced-motion preference also stops rotation.

Goals are saved under a separate browser storage key. Earlier browser verification changed its goal from 10,000 to 9,000 and confirmed it survived reloading. A fresh browser or Android installation starts at 10,000. The Android copy uses its own storage. Data is synthetic; there is no sensor, account or network connection. The APK requests no permissions and serves only its bundled page inside Android WebView. Android Back closes an open dialog, returns a selected metric/period to today's Steps, then exits the app.

Verification: rendered desktop page inspected against the supplied reference; Today/7D/30D and keyboard switching, goal saving/reloading and pause toggle exercised in the browser. `node verification/check.cjs` passes all four metrics across 30 dates and three periods, coherent hourly/sleep/food totals, complete previous periods, reference averages, a single animation loop, and reduced-motion/hidden-page stops. On the physical Galaxy S25 Ultra, installation, launch, orb swiping/tapping and all four metric views were checked. Local and installed APK hashes matched. The original Health app's installed package path remained unchanged. A full accessibility, performance, date-picker and device-compatibility audit has not been performed.

Rebuild with `python android/build.py` using the local Java 17 and Android SDK paths shown there, or supply JAVA_HOME/ANDROID_HOME. No external dependencies are downloaded. Preserve `android/signing/orbit-local.p12` for compatible local updates; this is a local development signing identity. Generated builds live under `android/build/`, and the latest APK is copied to `dist/Orbit.apk`.
