# Orbit — handoff (updated 11 September 2026)

## Current state

- **Installed on the Galaxy S25 Ultra (SM-S938B) at the user's request, 11 September 04:14:** build `android/build/20260911-041135`, `dist/Orbit.apk` SHA256 `a81b137d20403325dbd3e2236b68c22d5cfc96cb827d9d78ff51c21b62636337` — the notification and music-player batch in the next section on top of the five fixes. `adb install -r`, data kept, installed hash matched, not launched. Previous phone APK: `verification/music-morph-phone/phone-before.apk` (`5076c44e…`). Record: `verification/music-morph-install.json`.
- Earlier installs on 11 September: 03:09 build `20260911-030125` (`5076c44e…`, the five fixes; `verification/five-fixes-install.json`) and 01:01 build `20260911-005538` (`5eec8e21…`, music switching fix and performance pass; `verification/perf-install.json`).
- The user's acceptance on the phone is pending. Phone launch/navigation/recording/music control still need a fresh explicit request; read-only queries are fine.
- New-session entry point: `C:\HA\CLAUDE-ORBIT-HANDOFF.md`. Profiling tools: `verification/perf-tools/README.md`.
- **Installed on the phone at the user's request, 11 September 20:43:** build `android/build/20260911-203737`, `dist/Orbit.apk` SHA256 `7d8b32a3d700de0af983ea83981715a3423f9e1ab63efc57fe64789144adc01e` — the Body page and glass material batch below. `adb -s 192.168.0.210:38347 install -r`, data kept, installed hash matched, not launched. Previous phone APK (`a81b137d…`, build `20260911-041135`) backed up at `verification/body-premium-phone/phone-before.apk`. Record: `verification/body-premium-install.json`. Source changes are not committed or pushed. The entries below about 04:14 describe the previous install.
- **Installed at the user's request, 11 September 21:37:** `android/build/20260911-213518`, SHA256 `843ab25a6824d0ab4cb33a3ba273bec80343c1628024a629c8367a60f05d276e` — the Body refinement and final corrections below. `adb -s 192.168.0.210:38347 install -r`, data kept, installed hash matched, not launched. Previous phone APK (`7d8b32a3…`, the 20:43 build) backed up at `verification/body-final-phone/phone-before.apk`. Record: `verification/body-final-install.json`. This supersedes the 20:43 install above.

## Body refinement — 11 September 2026 (Claude, after the 20:43 install)

A bounded follow-up on the Body screen, requested after using the 20:43 build. Baseline: commit `569bb71` (clean). Captures: `verification/body-refine-390/` (incl. `fat-top.png`, `fat-bottom.png`), `verification/body-refine-320/`, frames `verification/body-refine-swipe/`.

1. **Four measurements, labels only.** The selector lists Weight, Fat, Muscle, Lean mass — Weight first, then smallest to largest — with no values; only the ring shows the active value. "Body fat" (%) is gone as a separate metric: Fat (`fatMass`) reads in kg, and its share ("18.2% of body weight") is the ring caption and a note in the History readout. History (chart, stats, inspection, change) tracks the kg value.
2. **Looping.** Swiping the ring, tapping its centre and Left/Right keys step round the row: after Lean mass comes Weight, and back. Two capsules one track-width apart let the selection leave one end of the selector as it enters the other; the second is hidden except during that crossing.
3. **Draggable glass selector.** Press the track and slide: after 6 px of horizontal movement the capsule follows the finger on a light critically damped spring (ω 26) with a slight stretch at speed, brightens while held, and the label beneath it lights up; labels never move. Release picks the label under the finger and the ring runs its move from where the capsule is. A mainly vertical start is left to page scrolling (`touch-action: pan-y`); taps on labels still work.
4. **Arrows removed.** The chevrons inside the ring are gone; switching is by ring swipe, centre tap, label tap or selector drag.
5. Capsule placement is measured from fractional column rects (rounded offsets drifted up to a pixel per column).

Verification: `check.cjs` PASS; `workout-details.cjs` PASS; rendered `ux-motion` (new assertions: four label-only measurements in order with no values or arrows, ring drag commits and loops both ways, short held drag springs back, centre tap loops, selector drag moves the capsule without moving labels or committing early and picks the label on release, Fat in kg with its share in ring and History), `focus-music`, `workout-details` at 390/320 px PASS; `check-live-background.cjs` PASS.

Final corrections: the History readout keeps the inspected kg value as its main line, puts Fat's "x% of body weight" on its own line below it, and keeps date and change as one metadata row whose items never break inside (stacked as two lines at 360 px and below). "Muscle" is now the label in both the selector and the ring (display copy only). The hero glow was inset to the narrow page padding, removing a 3 px horizontal overflow at 320 px. Captures and fit measurements: `verification/body-final-390/`, `verification/body-final-320/` (Fat and Muscle hero and History, Fat inspected on 13 Aug). All checks above re-run PASS. Build `android/build/20260911-213518`, `dist/Orbit.apk` SHA256 `843ab25a6824d0ab4cb33a3ba273bec80343c1628024a629c8367a60f05d276e`, v3-signed, assets verified inside the APK; **not installed**, not committed or pushed.

## Body page and glass material — 11 September 2026 20:37 (Claude)

Requested with the user's Body and Explore screenshots: surfaces that read as frosted glass, a tighter Body page that feels like one scene, first-class left/right switching between body measurements, a ring that means something, and an integrated History panel. Baseline: commit `e45f6ef` (tracked files were clean). Captures: `verification/body-premium-before/`, `verification/body-premium-after/` (`compare.png`), swipe frames `verification/body-premium-swipe/`. Summary: `verification/body-premium-result.json`.

1. **One glass material.** `.health-glass` in `health-pages.css`: a charcoal tint over a 22 px blur with the existing grain, a 1 px rim brightest along the top edge, and a soft drop. Its tokens (`--glass-tint`, `--glass-blur`, `--glass-rim`, `--glass-lift`) are shared with the Explore launcher frost, which is now slightly more translucent with a brighter top rim (geometry and behaviour unchanged). Glass is used only for the Body selector, the History panel and the header back button; reduced-transparency and forced-colours fallbacks are included.
2. **Body layout.** The header floats over the page and frosts (soft masked edge) only once content scrolls beneath it; the demo label aligns with the title. Order: ring (300 px) → selector → History → footnote. The "Tap the centre…" hint and the separate lean/fat key were removed; the selector carries those values.
3. **Ring.** 100 dots, each 1% of body weight. Purple is the selected measurement's share (fat for Weight, with lean in silver); the rest are dim. The caption inside says what the arc is ("82% lean · 18% fat", "45% of body weight", "14.0 kg of 76.9 kg"), and the label adds the date when an older reading is inspected.
4. **Switching.** The ring is a lens over the row of measurements. A horizontal drag moves the reading with the finger (8 px slop, starting from rest), the neighbour enters through a soft circular mask, the purple arc interpolates between the two shares and the selector pill travels with it. Past 38% of the lens width or a flick commits; otherwise it springs back. The settle is a critically damped spring (ω 19) from the release position and speed and can be caught again mid-way. The first and last measurement resist instead of wrapping. Taps on a selector entry, the ring's chevrons (hidden at the ends) or its centre (next, wraps as before) and Left/Right keys on the centre run the same move. Selector state, History and a polite announcement update at the decision, not after the settle.
5. **Selector.** A glass track of five equal columns, each with the measurement's name and its value on the inspected date; a lavender glass pill marks the chosen one. "Muscle" is labelled "Skeletal muscle" in the ring.
6. **History.** Recessed range track (44 px hit areas), readout (value, date, change since the range start) with a date stepper, chart with max/min hairlines, a gradient guide and a halo that grows while scrubbing, readings drawn as dots. 3M and 1Y draw weekly averages through the dots (the demo readings oscillate daily and drew a solid zigzag); the coverage line says so. Average, lowest and highest of the range sit under the chart. Real dates, the empty early year and exact-date inspection are unchanged.

Ambiguous points decided: only the five existing measurements (no invented BMI or body water); the selector sits under the ring as the compact summary row; swiping does not wrap, but a centre tap still cycles.

Verification: `check.cjs` PASS; `workout-details.cjs` PASS; rendered `ux-motion` (new assertions: ring drag commits, a short held drag springs back, chevron step, first-measurement resistance), `focus-music` and `workout-details` at 390/320 px PASS; `check-live-background.cjs` PASS (frost range 0, timer-only mutations, 0 px background change); `native-check.ps1` PASS; the built `assets/index.html` contains the new code. Swipe frames used CDP touches at 384 × 832 (the S25 Ultra's CSS size). New tools: `verification/perf-tools/body-shots.cjs`, `body-swipe-frames.cjs`.

Limits: headless Chromium with synthetic touches; the swipe feel and the frost on the phone's GPU need the user's check.

## Notification and music player — 11 September 2026 04:14 (Claude)

Requested after using the five-fix build. Backups with SHA-256 manifest: `verification/before-music-morph-20260911-034126/`. Summary: `verification/music-morph-result.json`.

1. **Workout notification.** On Android 17 the metric card already shows the running Duration, and the header chronometer repeated it. The header clock is now off whenever the metric card is used; older Android keeps the header clock and drops the text clock while it runs. Orbit character is added inside the Live Update template (a custom layout would lose the status-bar chip and lock-screen card): Orbit's planet-and-ring mark as the small icon (`res/drawable/orbit_notification.xml`), the Orbit purple accent, the activity glyph inside Orbit's sixty-dot ring as the large icon, and a spare metric slot showing when the session started. Its look on One UI 9 has not been seen yet; that needs a running workout.
2. **Transport controls.** Rounded glyphs in One UI's style; square 56/64 px targets with the glyph at the centre; a press shrinks and dims the glyph instead of showing the off-centre ellipse. Heart and shuffle were not added: the music app would have to provide those actions.
3. **Small player ↔ page-wide view.** One progress value (the `WorkoutFocus` morph) drives every way between them: tapping the small cover or title, the header chevron or the clock; swiping down anywhere on the music view (the seek bar and particle clock excluded); swiping up on the small player. The cover image travels between the page and the small cover, title, artist and controls move and scale, and the clock keeps its proportions. Parts of the other layout wait at their measured places and fade. On release the position and speed decide the result, and the move reverses from wherever it is. Frames write only transforms and opacity. The hidden cover is drawn at touch-down so the first frame does not wait for it. The page tint now lives inside the cover so it fades with the move.
4. **Album-art colours on Pause and Finish** in the page-wide view. The cover's most saturated hue gives a two-tone Pause (dark or white text, whichever reaches at least 4.5:1) and a tinted-glass Finish. Grey covers keep the neutral buttons, and the colours change with transitions.

Verification: `check.cjs`, `workout-details.cjs`, rendered `focus-music`, `ux-motion` and `workout-details` at 390/320 px, `check-live-background.cjs` and `native-check.ps1` all PASS; the Java also compiles against android-37. New rendered assertions cover mid-move positions, spring-back after a short drag, drag down to the small player, drag up to the page-wide view, centred glyphs without a halo, and Pause contrast. `measure12.cjs` at 4× throttle with real touches: tap-open 65 frames in 900 ms with a 16.8 ms maximum gap; tap-close and drag-close also 16.8 ms maximum, with no frame gaps over 20 ms. Compared with the previous build, the artwork above the clock differs only by sub-pixel edge anti-aliasing (the image now sits in its own layer), and the clock sits 7 px higher because the control row is 9 px taller. Build `android/build/20260911-041135`, SHA256 `a81b137d20403325dbd3e2236b68c22d5cfc96cb827d9d78ff51c21b62636337`, v3-signed, **installed** at the user's request 04:14.

Limits: swipes were verified with synthetic touches in Chromium; the notification's appearance and how the swipes feel still need the user's phone check.

## Five fixes — 11 September 2026 (Claude)

Requested by the user after using the performance build. Backups with SHA-256 manifest: `verification/before-five-fixes-20260911-021144/`. Summary: `verification/five-fixes-result.json`. The user calls the bottom bar the "live notif".

1. **The launcher closes with a downward swipe.** Before, only the bottom bar row took a drag, it needed 90 px on a fixed 180 px scale (the row sits too close to the screen edge for that), touches over the open launcher's shortcuts were ignored, and the drag direction was set by the first movement (a downward pull could open it). Now the whole open launcher is a drag surface, shortcuts and workout controls included, and a drag never presses them. The shell's top edge follows the finger 1:1, up always grows it and down always shrinks it, and a flick or half the growth commits. A downward pull on the bar of the open card stack folds the stack without also opening the launcher; the old check "pulling down on the bar opens activity choices" was replaced.
2. **Body launcher icon** is an outline figure (head and shoulders, 1.5 px stroke) matching the heart and dumbbell. The Body page's own dot ring is unchanged.
3. **The idle bar is named Explore** ("Body · Workouts · Health", 2×2 grid icon) instead of "Health", which repeated the Health shortcut above it. A live workout still shows its activity and time.
4. **First stack swipe or tap.** Measured headlessly at 4× throttle: a tap re-recorded all four frosted card shells, the grain tile and the deck background on the first moving frame, and a swipe did the same at release. Opening toggled the cards' `inert`, the deck scroller's `pointer-events`/`touch-action`, its edge mask and its overflow, and each of those re-records every shell. Now `#period-content` is always hit-testable and the scroller's `touch-action` is constant. `inert` and the `.is-scrollable` mask and overflow change in `settleDeckStructure()` once the stack rests, so that single repaint lands when nothing moves. The folded charts render at touch-down instead of on the first moving frame. Drags start from rest at the edge of the 8 px touch slop instead of jumping to the finger: the first frame moves 3.55 px for a 12 px finger move. Pixel comparison with the orb hidden in both trees and the old label and icon restored: folded, open and launcher 0 px. Mid-fold differs by 152 px (0.012 %, none over 40 levels) because the edge mask now waits until rest; mirroring the old mask timing gives 0 px.
5. **Music on the workout page.** The player stays on the plain workout view as one small row above Pause/Finish: a 48 px cover (from a 192 px copy, never enlarged), title, artist, and previous, play/pause and next. Tapping its cover or title opens the page-wide artwork view. A round chevron at the top right of the header, shown only in that view, returns to the workout page, and tapping the clock still toggles. The page artwork and tint fade out over 440 ms on the workout view and back in. With music on the workout view the dial gives up some height, so Pause/Finish stay on screen at 844 px. The old animated player exit (`stop(true)`) was removed.

Verification: `check.cjs` PASS (launcher expectations updated); `workout-details.cjs` PASS; rendered `ux-motion`, `focus-music` and `workout-details` at 390/320 PASS. New rendered assertions: launcher drag up, slow and short drags down, drag over a shortcut, the name not repeating a shortcut; stack drag starting from rest and keeping its structure until settled; small player, chevron, sharp cover, controls, and Pause/Finish on screen. `check-live-background.cjs` and `native-check.ps1` PASS. Build `android/build/20260911-030125`, SHA256 `5076c44e3a93f0442e5f756f13354ec3571d5fe793d16985b954442c7a51d722`, v3-signed. **Installed** on the phone at the user's request, 11 September 03:09 (hash matched, data kept, not launched; `verification/five-fixes-install.json`). New profiling tools: `verification/perf-tools/measure8`–`measure11.cjs`.

Limits: headless numbers are relative (software raster). A first-use cost that exists only on the phone's GPU (first raster or shader compile) cannot be reproduced here; confirm by feel and with a read-only `dumpsys gfxinfo` after use. The small player was verified with a browser music fixture, not the phone's media session.

## Performance pass — 11 September 2026 (Claude)

Phone frame statistics (read-only `dumpsys gfxinfo` of the user's running Orbit process, 13,405 frames): 15.2 % janky frames, p50 8 ms, p90 17 ms, p99 42 ms, 1,663 slow draw-command frames. Attribution was done in a CPU-throttled headless Chromium at phone geometry with tracing; numbers below are per scenario at 4× throttle and are relative, not S25 Ultra times. Raw results: `verification/perf-measurements/`, summary `verification/perf-result.json`.

Findings and changes — no visual change intended, nothing removed (checked backups in `verification/before-music-switch-20260910-234519/`):

1. **The deck fold repainted all four card shells on every frame.** Any per-frame change on a `.stack-card` (transform, opacity, clip-path, or an inherited custom property) re-resolved and repainted its `::before` shell — grain image, gradients, shadows — about 430 times per open/close. Each card now sits in a `.stack-motion` box that carries the per-frame transform, opacity, z-order and the fold clip (`overflow:hidden` plus height, the same square-bottom cut). The shell is a real child element `.stack-shell` with the identical declarations (the `.stack-card:before` rules were renamed) and its fold scale is set inline. Staged-reveal opacity/translate, the footer fade and the metric-icon fade are written inline to the exact elements instead of `--reveal`, `--details` and `--open` on the card; card children are compositor-promoted. Per two open/close cycles: style 864 → 165 ms, repaint 2,329 → 181 ms, main-thread busy 46 % → 25 %; shell repaints 428 → 16 (the two class toggles).
2. **Progress variables were set on ancestors of everything.** `--deck-progress` (was on the deck) and `--island-open` (was on the island) are set on the live bar and the island trigger — the only ancestors of their reader, the chevron — so the frost, shell, body and cards are no longer re-styled per frame. The hero's `--orb-y/--orb-scale/--readout-scale` are written as the same transforms directly on the canvas and readout. Style resolutions per fold cycle 14,121 → 2,576.
3. **Hidden chart work on every tap and metric change.** The daily and weekly charts live in folded cards that are invisible until the deck opens; `renderBars/renderLine` now run in idle time, or synchronously the moment the deck starts opening (tap or drag) and whenever it is open. `verification/check.cjs` exposes `renderCharts` and flushes it before its NaN assertions.
4. **Orb draw calls.** The 646 dots were 646 separate fills per frame; they are batched into 16 depth bands (same dots and radii; alpha quantised to 0.04 steps). `check.cjs` still counts 646 arcs.
5. **Live bar.** Whole-pixel shell radius while it grows (31 → 28 px in integer steps rather than a repaint per frame) and one forced layout in `measureDeck` instead of four: style 246 → 134 ms, repaint 409 → 284 ms per two open/close.

Not changed, because it is the accepted material: frost re-blur (`backdrop-filter`) while content moves beneath the live bar, the facts card frost and the utility island. GPU composite time is unchanged (about 2.9 s per two fold cycles at 4× throttle); reducing it would mean smaller blur radii or fewer frosted layers.

Verification: `check.cjs` PASS; rendered `ux-motion` and `focus-music` at 390/320 px PASS; `check-live-background.cjs` PASS (frost size stable, timer-only mutations, 0 px background change); `native-check.ps1` PASS. APK compiled and v3-signed: build `android/build/20260911-005538`, `dist/Orbit.apk` SHA256 `5eec8e21feee715220995be14b76c16ba003a4e572677a01ce944be8c20503dc` (includes the music switching fix; supersedes `1e2d98005a3cf686…`). Before/after pixel comparison at 390 px, DPR 2, orb canvas masked because its paused angle differs between runs: folded 0 px, live bar open 0 px, mid-fold 0.07 % and open deck 0.14 % of pixels differ by more than 8 levels (0.02 % by more than 40) — text and icon edge anti-aliasing of the promoted layers in headless desktop Chromium, which uses LCD text on opaque surfaces; Android text is grayscale either way. Screenshots and diff images: `verification/perf-visual-diff/`. **Installed on the S25 Ultra at the user's request** (`adb install -r`, data kept; installed hash matched; previous APK backed up at `verification/perf-phone/phone-before.apk`; not launched — see `verification/perf-install.json`).

Limits: measurements are relative; on-device confirmation is the `dumpsys gfxinfo` janky-frame ratio after the user has used the new build. The earlier experiments (paused compositor animations, device-pixel snapping) were measured, found not to matter, and removed.

## Music switching fix — 10 September 2026 (Claude)

Root cause of the "goes dark, then loads" track change: most players publish a new track's title first and attach its cover a moment later. The native bridge finished its first artwork pass with nothing, reported `art:""`, and the page removed the cover and the `music-focus` tint in that same frame; the real cover then arrived and faded in from the bare page.

Changes (checked backups in `verification/before-music-switch-20260910-234519/`):

- `android/src/com/mani/orbit/MusicSession.java`: after a metadata change the artwork worker keeps re-reading the same track's metadata every 250 ms for up to 2 s while no cover is available (`ARTWORK_GRACE_MS`). During that time `read()` still reports `artLoading:true` and omits `art`, so the page keeps the previous cover. Null metadata no longer reports "no artwork" instantly. A different track's metadata still starts its own pass immediately.
- `workout-focus.js`: a real track without artwork now fades the previous cover out (440 ms) instead of removing it in the same frame; the blurred backdrop uses a 48 px copy of the decoded cover instead of the full 2048 px image; the incoming layer gets two frames to rasterise before its crossfade starts; `--workout-art` carries the small copy.
- `health-pages.css`: `.music-art-layer` is compositor-promoted (`will-change:opacity`); the `.health-page` background colour transitions over 440 ms so leaving the art tint is not a cut.
- `verification/focus-music.html`: new assertions — a track change with pending artwork keeps the previous cover; an art-less track fades the cover out and releases it.

Checks: `node verification/check.cjs` PASS; rendered `focus-music` and `ux-motion` at 390/320 px PASS (Playwright 1.63 with the installed Chromium 1243); `verification/native-check.ps1` PASS. APK compiled and v3-signed at the time: build `android/build/20260910-235056`, SHA256 `1e2d98005a3cf686affaf9d26a3a17d42e2eaeb78da5daee2fbd09fccf9e7a63`; superseded by the performance-pass build above. **Not installed.** The user asked that nothing be installed or replaced yet; the phone at `192.168.0.210:44875` was only queried read-only (it still runs `6dfb41a4e867e51cd67b9983e30db71ca922c02c41c112fbcdd325f8f72ad883`). The previous APK is kept at `android/build/20260910-235056/previous-Orbit.apk`. Result summary: `verification/music-switch-result.json`.

Limits: the grace period is native behaviour and was not exercised on the phone in this batch; a provider that never publishes a cover shows the previous cover for up to 2 s before the fade. Audio buffering and cover availability remain the music app's. Nothing else in the app was changed; the audit scan found no other per-second or per-frame work beyond the intended 1 s timer tick, 1 s music poll and the always-on orb/dot animations.

Earlier state, 10 September 2026 (superseded by the sections above): the signed APK `6dfb41a4e867e51cd67b9983e30db71ca922c02c41c112fbcdd325f8f72ad883` was installed on Galaxy SM-S938B (`192.168.0.210:39389`); build `android/build/20260910-194200`; final launch WARM, 115 ms. The user said: “yo if its installed leave it to me”, ending agent-run phone checks at that time.

This is the current entry point for continuing Orbit. Read the constraints below before proposing or changing UI. Older notes, screenshots and result files are historical; they do not override these later decisions. The current task was to document the latest state and preferences, with no further phone interaction or implementation.

## Hard design constraints

1. **Keep the agreed identity.** The base app is black/charcoal with quiet silver/grey detail and a purple highlight. The user explicitly rejected changing the whole palette to a purple gradient. Frost references describe the material, not permission to copy their pastel colours. Actual album artwork may colour the focused music screen. Preserve the existing Orbit stack and font instead of introducing another visual system.

2. **Use the screen properly.** The phone app fills the display and hides system bars for the requested immersive view. Do not bring back a mock phone frame, surrounding black gutters or a disconnected framed app surface. The app background continues under the floating frosted bar; content remains scrollable clear of it. Supporting controls must remain readable and reachable on narrow screens.

3. **Make data clean and legible.** Lead with a useful visual summary, clear values and short supporting labels. The user rejected packed columns, awkward overlapping curved edges, heavy outlines and an overworked glass material. Use restrained frost, simple rounded panels and spacing to group information; avoid unnecessary dividing lines and decorative containers. They disliked log-first Body pages and a collection of repetitive full-page data lists. Relevant detail still needs to be available behind deliberate interaction.

4. **Keep full, purposeful motion.** The user explicitly rejected automatic reduced-motion suppression. Do not silently restore OS-preference gates. Keep manual orb pause and hidden/offscreen cleanup. Taps, swipes, selection pills, focus entry/exit and opening controls need visible, coordinated feedback. Follow the supplied Apple motion reference for continuity, timing and movement that can reverse from its current position. Fast snaps, remounting/shuffling content and background flicker are failures. Ordinary page changes retain short fades/small slides; the rejected CutTracker-style source-to-page/cloned-page transitions must stay removed.

5. **The originating control owns its expansion.** The three-dot circle itself expands into its inline options; do not add another circle or detach a popup. The latest request explicitly requires animated growth of the bottom live bar: one shell grows upward from a fixed bottom and its controls follow inside it. Preserve motion on rapid reversal. Do not revert it to the older instant-open implementation. Keep the blur texture and background stable while the timer runs. The two status dots and old grab handle were rejected. Flat Body / Workouts / Health icons stay normal size with single-line labels; do not restore raised icon bubbles or a separate panel floating above a short shell. The current filled Pause pill is intentional, so the shortcut-bubble rejection is not a blanket ban on rounded controls.

6. **Preserve Home's organisation.** The globe switches period on tap and metric on horizontal swipe; animate both the retained globe and its reading. The four folding panels are the summary, daily activity, recent period and comparison, not four unrelated metric pages. Keep direct card gestures and normal scrolling coherent. The selected bar's decorative top dot stays removed. An inspected chart value must fade after a short interval instead of lingering over the chart. The top of the scrolled deck needs an intentional soft fade, not an abrupt rectangular cutoff. Do not repeat Home's Steps / Heart / Sleep / Intake as extra Health overview tiles. The launcher destinations are Body, Workouts and Health; Oxygen is a compact expandable overview strip, not its own full page.

7. **Body must feel interactive and support long history.** Keep the tappable dot composition ring, focal value, animated metric selector, animated range pill and exact-date inspection. Preserve 7D / 30D / 3M / 1Y ranges. A year is a real calendar range; display sparse data on its actual dates and make missing history apparent. Do not stretch 90 records into a fake year. The user tried and rejected the raised 3D strip chart: retain the smooth flat 2D line with soft shading. Do not copy the rejected Samsung Body layout, or spread this chart treatment to unrelated components without a request. Muscle is part of lean mass; do not stack overlapping body measures as independent shares of a total.

8. **Keep the Sleep card and remove the unwanted interaction.** The user clarified that the unwanted thing was text selection and Android's copy/translate menu, not the Sleep summary itself. Noneditable app content must not select on long press; actual inputs must remain editable. Tapping the Sleep summary opens a night timeline showing when each stage occurred, with start/end/duration and correct overnight dates. Selecting a stage highlights all its matching intervals. Preserve scrubbing, interval stepping and the subtle selected-interval outline. Do not restore the rejected dotted inspection cursor or dotted timeline grid.

9. **Workout timers should be expressive without clutter.** Use dot-matrix focal readings and the playful touch-responsive countdown/clock particles. Keep the activity → setup/target → countdown → live workout → pause/resume/finish → saved detail flow. The active workout's large walking graphic was rejected in favour of a tappable dot pattern; normal activity-selection glyphs can remain. Remove slogans such as “In your rhythm”, “Find your rhythm”, “Your pace, your time” and other filler. Keep only useful labels and instructions. Focus uses the numbers themselves, with no little orbit above/behind them or unnecessary extra timer labels. Pause freezes the same clock, makes it slightly darker and a little smaller, then resumes smoothly; it must not shuffle the timer/player layout.

10. **Preserve the liked full-screen music composition and fix latency at its source.** Real album artwork fills the width from the top edge and fades through the focused workout background, with a smaller clock and reachable controls. Do not turn it back into an isolated album card or low-resolution enlarged thumbnail. Use the best shared source, retain the previous decoded cover until the next is ready, crossfade covers and keep artwork anchored during control movement. Native metadata/artwork events should avoid waiting for the next poll; eligible upcoming covers may be warmed. Do not promise audio preloading or artwork the provider does not expose. Keep the music-app launch control a plain icon. Real paused tracks remain available; no-track, permission and error states hide the player. Missing art retains real metadata and supported controls. Do not add pretend heart/shuffle/repeat controls. Music transport and workout Pause/Finish are separate actions.

11. **Use rich, organised workout information and neutral Android presentation.** Samsung's workout flow and grouped summary/detail density are references for useful information, not an instruction to copy the whole Samsung app or its rejected Body pages. Show actual duration, pauses, target and supported route/movement/energy detail clearly. Retain old time-only records and explicit missing-data states. The blue workout notification and purple app icon were rejected. Keep the charcoal/silver Orbit mark, monochrome-compatible icon and neutral live notification with useful organised metrics and independent Pause/Resume/Finish. Notification pace stays omitted; its fixed target must not become a negative countdown.

12. **Keep information honest.** This standalone copy is not yet a complete replacement for Samsung Health. Health/Body/sleep histories are labelled samples; app-owned workout records are real and separate. User-supplied screenshots are design references, not permission to import their measurements. Never use demo body weight for workout calories, invent sensor/GPS readings, fake maps, fabricate earlier history or label a historical reading live. Use optional session weight only for the labelled energy estimate. Watch/native Health integration and physical outdoor accuracy require their own work and verification.

## Decisions that supersede older directions

- 11 September (standing rule from the user: "from now on once done install on phone and push to github"): when a batch is finished and verified, build, install on the phone (back up the phone APK, `install -r`, verify the hash, do not launch) and commit + push `main` without asking. Launching, navigating, recording, workouts, permissions and music control still need an explicit request. This supersedes the older "installing needs a fresh request each time" and "commit or push only when asked" notes.

- 11 September (Body refinement): the Body selector shows labels only — Weight, Fat, Muscle, Lean mass in that order — and the row loops; "Body fat" is no longer a separate metric (Fat reads in kg with its percentage alongside); the ring's chevrons are removed and must not return in another form; the selector capsule can be dragged. This supersedes the 20:37 selector that listed values and the edge resistance.

- 11 September (20:37): the Body metric selector moved under the ring and lists every measurement's value; the ring itself is swipeable (lens with a travelling pill) and each of its 100 dots is 1% of body weight. The "Tap the centre…" hint and separate lean/fat key are gone. Long chart ranges draw weekly averages through daily reading dots; the flat 2D line with soft shading stays. One shared glass material (`.health-glass` and its tokens) now covers Body's containers/controls and the Explore launcher frost — restrained, not applied to every surface.

- 11 September (04:14): the page-wide music view's Pause and Finish take colours from the album art (constraint 1 already lets artwork colour the focused music screen). Transport glyphs follow One UI's rounded style without halos. The workout notification now carries Orbit's mark and purple accent inside the Live Update template instead of neutral grey. A fully custom Orbit notification layout would lose the Live Update (status-bar chip and lock-screen card); that remains the user's decision.
- 11 September (five fixes): the idle launcher is named **Explore** and its Body icon is an outline figure. The launcher shell follows a finger up and down from anywhere on it, and a downward pull never opens it; this supersedes the older tested "pulling down on the bar opens activity choices". The plain workout view keeps a small music row above Pause/Finish, while focus keeps the page-wide artwork composition of constraint 10 unchanged. The header chevron appears only in focus.
- 11 September: the user explicitly asked for the performance build to be installed ("run it for me"); it was installed over wireless ADB with data kept. That authorisation covered the install only — launch, navigation, recording, workouts, permissions and music control still need a fresh request.
- 11 September: the user reaffirmed zero visual compromise — improve performance and UX without removing or toning down anything. Frost, full motion, dot timers and page-wide artwork stay; performance work must prove itself with the pixel comparison.
- The latest bottom-bar request restores proper animated shell growth. Earlier notes saying automatic launcher expansion should be instant are obsolete. General page-clone morphs remain rejected.
- The latest explicit full-motion preference overrides older reduced-motion branches and skill defaults. Manual pause and lifecycle cleanup remain required.
- The raised Body chart was a rejected trial; the flat shaded chart is the retained direction. The frost reference never authorised a palette change. Flat launcher shortcuts do not mean removing the selected Body pills or workout Pause button treatment.
- The latest phone instruction ends the earlier authorisation for an agent-run short UI sweep. Installation is confirmed; leave launch/navigation, screenshots, recording, new workouts, permission changes and music controls to the user unless they explicitly request another physical check. Do not restart the reconnect heartbeat or create a new monitor.

## Reference use and acceptance

Use the user's actual supplied references for the relevant component. Do not infer approval of every element in a moodboard, an old implementation screenshot or a test capture. An installed APK and passing automated checks do not equal the user's visual approval.

- Original Orbit starting reference: `reference.png` (historical baseline; later decisions above take precedence).
- Material and clean-data references: `verification/material-reference/user-frosted-glass.png` and `user-clear-cards.png`; the adjacent rejected screenshots document the extra-circle/disconnected treatments.
- Apple-style timing reference: `verification/motion-reference/REVIEW.md` and its frame sheets. Its descriptions of old code are historical; use the video evidence for motion.
- Samsung workout-flow reference: `verification/visual-health-reference/workout-contact.png`.
- Nineteen original supplied images are now preserved unchanged under `verification/design-references/`, covering Nothing layout inspiration, full-screen music art, icon/workout-detail references and the rejected Body, Sleep, chart, cutoff and live-bar treatments. `README.md` identifies how to use them; `manifest.json` records their hashes. They are verification references and are not bundled into the app.

Before continuing, read this handoff and the relevant reference, inspect the current affected code, then make a recoverable, focused change. Do not reopen settled style choices or ask repeatedly whether to continue authorised work. Keep useful functions and data intact while simplifying implementation. Update this handoff after each coherent batch with source changes, checks, build/install status and remaining limits. State implementation, automated verification, physical checks and user acceptance separately. Address visual regressions against the specific component rather than redesigning unrelated pages or using a generic style label.

## Completed changes

1. Pause/resume keeps the same timer, canvas and player; frozen time eases to 62% brightness and 96.5% size, then restores.
2. The live bar grows upward with interruptible movement and a fixed bottom. A single clipped shell contains its controls. Both status dots and the old grab handle are removed; icons do not scale during entry. Pause has a filled pill.
3. Orb period and metric selection animate the globe and reading without replacing the canvas.
4. The Home chart value badge fades after inspection while its accessible value remains available.
5. The scrolling card edge fades into the app background.
6. Body metric/range pills slide; 7D/30D/3M/1Y charts preserve actual record dates and show missing earlier data. The 90 sample dates are not stretched over a year.
7. Native metadata/artwork events refresh the player immediately; background artwork decoding and next-three eligible local-cover prefetch avoid extra polling delay. Progress/fallback polling stays enabled.

The last flicker correction fixes two unnecessary redraw paths. A constant-size blur layer is clipped by the growing shell, and unchanged timer/state data no longer rewrites icons, labels, geometry or card styles. Manual motion controls and lifecycle cleanup remain; the user's always-enabled motion preference is preserved.

## Verification

- Node interaction/model and workout-details checks: PASS.
- Rendered UX, focus/music and workout-details checks at 390 and 320 px: PASS. UX reran after the final shared measurement change.
- Background regression at both widths: blur size changed by 0 px during expansion (previously about 226 px); only timer text mutated through repeated timer/native-style refreshes; the sampled unoccluded background was pixel-identical across reversals. The headless browser did not reproduce the original visible flash before the patch, so these checks verify the removed redraw paths rather than physical visual acceptance.
- Native artwork dimensions/aspect/no-upscale, altitude, resume tokens and geographic-distance checks: PASS. Java compile, APK v3 signature and bundled-source verification: PASS.
- Read-only actual media check on the preceding native-identical build: first read 1 ms, artwork-ready event received, current cover 544 × 544, no media transport commands. BitChord supplied no eligible upcoming local covers, so positive provider prefetch remains unqualified.
- Earlier physical seven-fix sweep: focus running/paused/resumed screenshots, actual music art, launcher opening/closing video, Finish/save and history. Its owned Strength test was completed at 03:31 active time; history grew from 13 to 14 records. No unrelated records were removed.
- Latest APK installation and exact installed hash: PASS. Idle Home capture: `verification/seven-ux-phone/flicker-current.png`. No new workout was started; the attempted follow-up check stopped when the user requested control. Running-workout physical qualification of the final patch is pending user review.

Evidence: `verification/seven-ux-result.json`, `verification/seven-ux-browser/`, `verification/seven-ux-phone/`. The earlier `pause-resume.mp4` does not capture the exact pause transition and must not be used to claim it does. Older result files describe older builds.

## Boundaries and recovery

11 September backups: `verification/before-music-switch-20260910-234519/` holds the pre-change `index.html`, `signal-orb.js`, `workout-focus.js`, `health-pages.css`, `MusicSession.java`, `focus-music.html`, `check.cjs`, `HANDOFF.md` and `README.md` with a SHA-256 manifest; the pre-install phone APK is `verification/perf-phone/phone-before.apk`. Each build folder keeps `previous-Orbit.apk`.

Work only in this standalone Orbit copy. `C:/HA/HEALTH APP` remains untouched. Preserve the signing key and all existing app data. Do not manipulate the other connected device. No permission grants or music transport controls were used. The reconnect heartbeat remains unchanged; no background phone checks are running.

Use checked backups in `verification/before-seven-ux-fixes-20260910-183639`, `verification/before-live-bar-review-20260910-191140`, and `verification/before-live-background-20260910-193826`. Each build retains the previous APK.

Health/Body/sleep histories remain labelled demonstration data. Actual app-owned workout history is separate. GPS accuracy, broad music-provider compatibility, Watch integration and replacement of native Health remain unqualified. Queue cover caching cannot preload audio or artwork a provider does not expose.
