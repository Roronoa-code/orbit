# Orbit handoff — stopped after the requested phone installation

**Later update:** The owner resumed a phone UI-only parity pass, then explicitly paused again. Read [UI-PARITY-PAUSED-2026-09-15.md](UI-PARITY-PAUSED-2026-09-15.md) first for current source, checks and resume point. The installation record below remains valid; this later UI work has not been installed on the physical phone.

Updated 15 September 2026, after the 22:49 BST installation. This is a handoff, not a completion declaration.

## Stop instruction and exact resume point

The owner requested: finish only the safe local check/fix already underway, write a thorough handoff, build/install the current phone update, and defer all other testing and addendum work until another day. That batch is finished. Do not resume broad testing, research or implementation automatically from this handoff.

On an explicit resume, begin with the unchecked W1/W2/W3 route-review items in `RESEARCH-ADDENDUM-CHECKLIST.md`. The shared Watch navigation/shutdown fix below is implemented and passes its focused checks; it does not need to be rediscovered. Finish missing route-state/gesture coverage in coherent batches, then group relevant regressions. Do not use the historical full-suite counts as current-source results.

The native goal remains incomplete. It is not technically blocked from further local work; the stopping reason is the owner's instruction. Public/store approval is not required for this personal build. Physical Watch access has not been authorized. The phone authorization for this wrap-up was installation only, and that installation is complete.

## Current source and artifacts

| Item | Value |
|---|---|
| Native root | `C:/HA/design-concepts/signal-orbit-steps/native` |
| Shared existing Android backend | `C:/HA/design-concepts/signal-orbit-steps/android` |
| Older source foundation, preserve | `C:/HA/HEALTH APP` |
| Current complete native source fingerprint | `59dbf290f43aeb9c420e2d4aa2f778323cc87f26b5cd29af7c1867c928626869` |
| Current manifest/source file hashes | `verification/wrapup-20260915/checkpoint.json`, `source.json` |
| Signed phone artifact | `verification/wrapup-20260915/Orbit-native-phone-20260915.apk` |
| Signed and installed phone SHA-256 | `0949788006f61045ac367d1061eadf9d149ad4658054cf2383af3038d6e673e4` |
| Original and new certificate SHA-256 | `f8d3ef88936a1f88ac164bffcae80f677d66c8a823a9985f3ca7bdf975f58761` |
| Original phone APK copy | `verification/wrapup-20260915/installed-before.apk` |
| Original APK SHA-256 | `6cd4de84c283aea70cc202e31a782af44018ab01e673d3933ce20d2c50e32452` |
| Original published local file | `../dist/Orbit.apk`, unchanged by this batch |
| Current debug Wear/test APK hashes | Listed in the wrap-up checkpoint and final focused test manifest |

The source fingerprint covers production/test source and native build inputs; Markdown and verification output are not part of that fingerprint. The full file map is retained. This is a dirty working tree, not a Git commit. No commit, reset, push, broad cleanup or unrelated-file overwrite was performed.

The original Android key already existed at `../android/signing/orbit-local.p12`. The release candidate was signed with that key after comparing the installed APK certificate. No key was regenerated. The normal native debug signer remains a different identity; do not install debug APKs over the phone or use mismatched phone/Watch signing for paired Data Layer tests.

## Installation result and device boundary

- Target was explicitly `192.168.0.210:44981`, confirmed model `SM-S938B` (S25 Ultra).
- Package `com.mani.orbit` upgraded from versionCode 1 / versionName 1.0 to versionCode 2 / `2.0-native-dev` using `adb install -r`.
- Installer returned `Success`; the installed `base.apk` SHA-256 matched the signed candidate exactly.
- The original first-install time, `2026-09-09 20:57:51`, remained. Last update became `2026-09-15 22:49:06`.
- No uninstall, clone, package-ID change, clear-data, permission modification, screen unlock, launch, navigation, screenshot, recording, workout, music control or phone runtime test was performed.
- Read-only device operations were limited to connection/model/package metadata, pulling the pre-update APK, and checking installed package/hash/version after installation.
- No physical Watch operation was performed. Local Watch instrumentation used only emulator-5582.

The in-place update retained the app's Android data directory; its contents and first native launch were deliberately not inspected on the phone. Installation does not establish that the user's health history has rendered or migrated correctly at runtime. The old APK is retained as evidence, not permission for a downgrade or data-erasing rollback. Do not reinstall version 1 over version 2 without an explicit compatible rollback plan.

## What the current local batch completed

1. Removed the second swipe-dismiss owner in `WatchHeartActivity`. `WatchEnvironment` now supplies the only shared native Back surface. Idle raw-mode Back returns to the picker; leaving an explicitly active continuous recording does not stop its foreground service.
2. Reproduced a Recovery page jump when newly synced data arrived while a finger was held down. The displayed context now waits for release/settling, then uses the selected date rather than a shifting list index. Held Details taps also retain the intended date through date insertion/removal.
3. Removed an invisible Recovery detail Back step when all days disappear. Native Back then reaches the outer route immediately.
4. Found and fixed a real shared HR/raw shutdown defect. Startup cancellation could finish IO cleanup and then skip the final idle/foreground transition when returning to a cancelled coroutine. The whole final cleanup and state transition now run inside `NonCancellable`; IO work stays off the main thread.
5. Updated the existing sweat test's lease acquisition to wait up to five seconds. Background sync briefly uses the same lease for orphan recovery; the old immediate assertion failed before the sweat session opened. A genuinely leaked lease still fails the bounded wait. Production sweat behavior was not relaxed.
6. Extended native History, Recovery and continuous-sensor checks through the actual shared Back shell. The new foreground-service check uses the real sensor gate and service, with zero fabricated samples.

Current changed production files in this batch: `WatchHeartActivity.kt`, `WatchRecoveryActivity.kt`, `WatchHeartService.kt`. Test changes: `WatchRawSensorTest.kt`, `WatchHistoryTest.kt`, `WatchRecoveryTest.kt`, `WatchSweatTest.kt`. Navigation mappings are documented in `WATCH-LAYOUT.md`.

Recoverable pre-edit files and the preceding source snapshot are in `verification/watch-navigation/before-20260915T212927Z`. This includes the service file added before the shutdown repair and backups of all edited existing documents. No dependency/build directories were copied as source backups.

## Exact verification evidence

| Evidence | Result and scope |
|---|---|
| `verification/watch-navigation/20260915T214804Z/wear-checks.json` | Final current-source focused native Wear run: **13 passed**. Raw 3, Recovery 4, History 2, sweat 2, heart 2. Exact installed debug/test hashes and emulator identity included. |
| `verification/watch-navigation/wrapup-fixed-build.log` | Current-source `:wear:assembleDebug`, `:wear:assembleDebugAndroidTest`, `:phone:assembleRelease` succeeded under existing strict dependency verification. |
| `verification/wrapup-20260915/checkpoint.json` | Current source/artifact hashes, focused result, exact install evidence, and explicitly deferred gates. |
| `verification/wrapup-20260915/phone-install.log` | Phone in-place installer returned Success. Post-install hash/version confirmation is recorded in checkpoint and this handoff. |
| `verification/watch-navigation/20260915T213116Z` | Retained initial 8-test reproduction: incoming Recovery day replaced held content. Not a passing result. |
| `verification/watch-navigation/20260915T213350Z` | Intermediate focused 8-test pass before later coverage/shutdown changes. Historical only. |
| `../verification/native-runs/20260915T213530.490980Z-wear/manifest.json` | Full Wear run: 43 tests, one initial sweat lease-acquisition failure. The failure was before session construction. |
| `../verification/native-runs/20260915T214054.997847Z-wear/manifest.json` | Full Wear run: 44 tests, one failure in the new sensor shutdown check. Also records five passing developer checks and build/provenance checks. |
| `verification/watch-navigation/20260915T214618Z/wear-checks.log` | 11-test intermediate run retained the shutdown failure. Changing cleanup test ordering alone was insufficient; production boundary was then fixed. |

No current full-suite pass is claimed. No new phone instrumentation run, release lint or Wear release build was run after the final shutdown fix. The phone was built and installed as explicitly requested despite deferred physical qualification. The new capture file from Recovery is test evidence only; this wrap-up did not conduct another visual/motion acceptance review.

Useful earlier verified checkpoints, with their own source identities:

- `verification/samsung-import/native-raw/checkpoint.json`: source `481d6ce406f09f7668fa4430a24fe586b0fb425ac29712ed2ce4880deb5edf61`; 18 phone / 6 Wear / 5 developer checks; both debug/test/release builds; zero release-lint errors; four native captures inspected. Original source-reuse files preserved. This predates the current Watch-only fixes.
- `verification/samsung-import/native-sweat/checkpoint.json`: source `2c9de1fd8ea75e63ff175a29e5774d0a4783d7264ea38e78170fcfe3a2cfe847`; 10 phone / 4 Wear focused checks, build/lint and render evidence.
- `verification/samsung-import/native-heart-ibi/checkpoint.json`: source `621aa050aa390bcf300c148d006b3cb4f0a03b41abf1150dacef881f42837633`; 14 phone / 4 Wear focused checks, build/lint and render evidence.
- `../verification/native-runs/20260915T191221.672377Z-phone/manifest.json` and `20260915T191519.631173Z-wear/manifest.json`: historical full **99 phone / 35 Wear** regression. These are not the current source.

## Preserved completed implementation

The chronological checkpoints in `MIGRATION.md` remain authoritative over its old architecture-audit table. Do not restart work because the early table says a subsequently implemented feature is pending.

- Native Android Compose and Wear Compose architecture replaces HTML presentation while reusing validated Android backend/storage where appropriate. Phone native navigation, retained lifecycle state, Settings/profile, Home/Health/Measurements/Sleep, workout/history/media routes and shared gestures are implemented with local evidence; complete physical parity and acceptance remain open.
- Preserve charcoal/lavender, phone Manrope, independently sized/reordered illustrated Health cards, dot visuals, unboxed Measurements trend, approved five-minute Sleep display with original totals, circular date selection, fixed-size workout/music controls, full-screen artwork and the single slightly frosted floating navigation material.
- Direct Samsung import remains separate from source-qualified Watch originals. Native profile/settings and measurement consent/quality states remain. Imported Samsung truth must not be replaced with synthetic readings, demo sessions or inferred physiology.
- Watch Health Services collection, explicit workout ownership, background service behavior, passive/readings journals, sleep/recovery context, battery/device context, Tiles, complications and ambient routes are implemented. Physical cadence/accuracy and host behavior remain to qualify.
- Shared authenticated Data Layer includes durable journals/outboxes, exact-hash receipts, replay/duplicates, large callback transport, per-family compatibility and source/clock quality handling. Phone/Watch presentation shares the model; paired radio and two-version behavior remain to qualify.
- Reused Samsung foundation supplies BIA/oxygen/temperature, ECG, optional HR/IBI, running sweat and continuous raw optical/movement/temperature paths. Real status, failed fields, original timing, sample bits and pending capture preservation survive storage/replay. Raw PPG does not fabricate HR; acceleration does not fabricate steps; temperature is not core body temperature.
- Native retained-backdrop optics, separate transparency/contrast settings, runtime quality tiers, draw isolation, semantic command/haptic ownership, bounded private diagnostic traces and deterministic local replay are implemented. Dependency locks/checksums, developer commands and accurate transport disclosure are retained.

## Every remaining addendum area

The addendum is `C:/Users/abdul/Downloads/Orbit_Research_Addendum_2026-09-15.md`, SHA-256 `97f829dd0ff2028e10a48d1ca8e66055885a5f821b65b5aaf506788bd849d135`. All 13 requirements remain reconciled in `RESEARCH-ADDENDUM-CHECKLIST.md`. `verification/wrapup-20260915/remaining-addendum.json` is an exact snapshot of all **16 unchecked checklist items**, including the Samsung qualification item. Preserve both original and added requirements.

| Requirement | Remaining work / acceptance |
|---|---|
| W1 — attention | Finish consistent route hierarchy/optional-metric behavior and review every route's normal, absent, stale and failure render. Existing Today/pulse/History/workout matrices do not establish every other route. |
| W2 — circular geometry/type | Shared interactive/ambient geometry and local size/text matrices are implemented. Qualify real Watch insets/fonts/round clipping and finish route failure-state review. |
| W3 — gestures | Actual route ownership is mapped; current fixes pass focused tests. Finish exhaustive per-route cancellation, rapid reversal, detail return and crossing-control combinations; later qualify OEM edge/rotary, predictive Back and accessibility. |
| W4 — surfaces | Qualify actual Watch-face required-only, monochrome and ambient rendering, stale/unknown expiry/fallback and correct entry destinations in every host format. Local push budgets/semantics do not prove host timing or power cost. |
| W5 — conditional widgets | Verify current official API/dependency and actual Watch OS/host eligibility. If supported, adapt existing glance semantics with legacy Tiles/providers retained; otherwise record concrete not-applicable evidence. No speculative forced OS upgrade. |
| W6 — motion/haptics | Local event ownership and quiet/reduced-motion paths are implemented. Qualify physical feel, requesting-device-only confirmation, paired timing, ambient/background suppression and OEM accessibility input. |
| G1 — lensing | Qualify S25 optics, readability and actual frame pacing; obtain user acceptance. Preserve approved frost and fixed foreground geometry. |
| G2 — readability | Independent settings/system policy and local combinations are implemented. Physical S25 readability/motion review and user acceptance remain. |
| G3 — quality | Measure sustained S25 pacing, real thermal/power availability and battery cost; calibrate provisional missed-frame/recovery windows. Recording/sync must remain independent of visual quality. |
| R1 — diagnostics/replay | Local causal traces/replay and privacy allowlist are implemented. Measure physical trace/frame overhead with the performance gate; emulator timing is not hardware evidence. |
| R2 — compatibility | Test real paired Data Layer with two app versions, both update orders, capability propagation, lost queries/ACKs, reconnect/backlog and controls/context. Local codec/journal tests do not prove radio delivery. |
| R3 — tools/provenance | Implemented and locally verified. Run the existing commands against final source after remaining coherent work; preserve failure evidence and artifact/source/signing identity. No new infrastructure is requested. |
| R4 — privacy copy | Product-copy delta implemented/locally verified. Preserve explicit Google relay versus local-storage distinction. Physical routing/delivery acceptance belongs to original paired gate. |

## Remaining original-goal work and tests

1. **Current-source stability and complete native parity:** finish missing UI/functionality/interaction review, then grouped phone and Wear regression; current Wear release and both release-lint checks; migration/persistence on actual first native launch when authorized. Verify loading/error/empty/source states, fast reversals, card drag/resize/shuffle, Explore/live bar, chart/date selection, media artwork/controls, profile reuse and countdown/background lifecycle. Avoid treating a passing screenshot or syntax check as motion/runtime acceptance.
2. **Real paired lifecycle/data:** authenticated phone ↔ Watch delivery, offline and long backlogs, reconnect/replay/duplicates/conflicts, simultaneous starts/single ownership, mirrored controls and stale/rejected commands, accepted commit before confirmation, sleep/context publication, clock/reboot/DST handling, ongoing recording through background/screen-off and battery/device-state freshness. Use the same original certificate on both physical packages.
3. **Actual Samsung/Health Services qualification:** obtain explicit Watch authorization before any connection/install/control. Confirm supported trackers, SDK developer-mode/service access, consent/permission denial and revocation, real sensor quality/calibration/contact/accuracy, screen-off cadence, shared sensor arbitration, BIA/profile preconditions, ECG/partial saving, HR/IBI/raw callbacks, sweat estimates, low-storage/cleanup/recovery and paired history. Public partner approval is not a private-build blocker; actual supported access is still unverified.
4. **Physical product acceptance:** S25 and Galaxy Watch Ultra frame pacing, sustained battery/thermal cost, optical/readability quality, touch/haptics/rotary/system Back, round-host/ambient/Tile/complication behavior, large text/TalkBack and user visual approval. Native installation alone closes none of these. Phone motion or runtime tests require fresh authorization after this install-only stop.

No remaining requirement has been dropped because it requires hardware or because the owner requested a stop. Unsupported capability results must be explicit and source-backed; do not invent data or silently broaden permissions. Keep the user's actual Samsung history authoritative and the original source-reuse files intact.

## Resume commands and operational notes

After the owner resumes work, first read this handoff, the unchecked checklist and the latest relevant `MIGRATION.md` section. Recompute `dev.source_snapshot()` and inspect current source/dirty files; do not assume this artifact still matches a later checkout. Read/backup any new dirty files before editing.

The existing developer runner is `dev.py`; its default/doctor behavior is read-only. Python is `C:/Users/abdul/AppData/Local/Programs/Python/Python314/python.exe`. Java 17 is `C:/HA/HEALTH APP/.local/toolchains/jdk-17.0.20.1+1`. ADB is under `C:/Users/abdul/AppData/Local/Android/Sdk/platform-tools`.

Only after the remaining coherent implementation/review batch, use the existing explicit local commands from the native directory:

```powershell
& 'C:/Users/abdul/AppData/Local/Programs/Python/Python314/python.exe' -B -X utf8 dev.py full --module phone --serial emulator-5580
& 'C:/Users/abdul/AppData/Local/Programs/Python/Python314/python.exe' -B -X utf8 dev.py full --module wear --serial emulator-5582
```

Verify those are still the intended local emulators first (HealthAutonomousApi36 and HealthWearApi35). Do not run both test processes against one emulator. Full module checks build/install/test debug artifacts; release builds and release lint are separate Gradle tasks. No broader test or build process from this wrap-up remains running.

The focused runner `verification/watch-navigation/run.py` is retained for reproducing this batch. Final invocation selected `WatchRawSensorTest WatchRecoveryTest WatchHistoryTest WatchSweatTest WatchHeartTest`. Do not rerun simply to recreate already-recorded proof unless source changes or a new failure warrants it.

Do not overwrite `../dist/Orbit.apk` with a development candidate, uninstall the user's app, create a clone, change permissions, start measurements/media/workouts, test the phone, or touch a physical Watch from this handoff. The authorized installation has already succeeded. All further work is deferred.
