# Orbit — current standalone copy

Installed and phone-tested: `android/build/20260912-232234`, `dist/Orbit.apk` SHA256 `9c402a8e89d03a3dcc71ece9f57ebca93dc216a10c2a2bf4aade75cfb2bd904b`. Includes reliable Body swipes and selector drags, a full Weight ring, fog feathered above and below the Home card edge, compact durable Settings, week/day workout history and lighter music transitions. [Checks and limits](verification/player-settings-20260912/RESULTS.md).

The user released the phone for installation and testing on 12 September. The final installed APK hash matches, the separate Audit app was removed, playback and display settings were restored, and the main app's existing workout survived. Read `HANDOFF.md` before continuing; no background device work is running. Functional checks passed; occasional frame hitches and source-limited artwork remain documented.

## Current behaviour

- The Settings gear opens a full page for name, birth date, height, weight, daily goal, motion and music access. Profile, goal and Reduce motion use acknowledged Android storage, migrating existing browser values once; browser previews use localStorage. Saved weight prefills all new workout types, with an optional session override; older workouts keep their own weight. Profile measurements are never presented as recorded Body history.

- Surfaces share one frosted material family, with restrained rims and sharp foregrounds. Selector tracks carry one backdrop sample and their moving indicators use the same tint/light family. The displacement lens and focus-only white pill are removed. Matched black/text/artwork and moving-underlay pixel checks verify actual diffusion.
- Every selector — Body measurement, Body history range, the workout target segments, the timer reading and the workout chart tabs — runs one engine. A press answers from the indicator actually on screen, holding another option leans at most a tenth of one slot, dragging follows the finger without tweening, both straddled options react to the real overlap before release, walls compress the stretched shape, and the release projects a bounded distance and commits once. A cancelled gesture, or a release away from the track, commits nothing; vertical gestures still scroll and keyboard activation still works.
- Pausing preserves the timer, particle canvas and music player. The clock freezes, dims its digits to 82% and shrinks to 96.5%; resuming restores it smoothly.
- The bottom launcher grows upward as one clipped surface, with a fixed bottom edge and controls that enter after the shell grows. Reversals preserve movement. Its shortcuts are one compact rail (Body / Workouts / Health, 22 px icon beside the label) above the 62 px row, so it opens to about 134 px when idle; the Explore subtitle fades and the title settles as it opens. During a workout the rail is followed by Pause/Resume beside Finish & save and a full-width Open workout (about 246 px), and the row keeps the activity, status and elapsed time. Its glass is a lighter charcoal tint (42%) over a 26 px blur, a thin rim and a shadow cast by the shell. The shell follows a finger up or down from anywhere on it, and a drag over a shortcut never presses it.
- The live blur texture keeps its full size while the shell reveals it. Timer ticks update changed text only; unchanged native state refreshes no longer relayout the launcher or the background cards.
- Orb period taps and metric swipes animate the retained globe and reading. The chart inspection badge fades after use. No page-wide fade overlays the Home globe or moving cards. The card stack starts moving from rest under the finger; its hit testing, focus and scroller change only once it rests.
- Body is one scene: a 100-dot ring (each dot is 1% of body weight; purple is the selected measurement's share, with Weight filling the full ring) with the dot-matrix reading inside it, a glass selector of four labels (Weight, Fat, Muscle, Lean mass — Weight first, then smallest to largest) and a frosted History panel. Only the ring shows values; Fat reads in kg with its share of body weight beside it. Swipe the ring, tap its centre, tap a label, or press the selector and slide across the labels (the glass capsule follows the finger, the labels stay put, release picks): the reading slides through the ring, the purple arc grows or shrinks and the capsule travels together, following the finger and settling from wherever it is released. The row loops: after Lean mass comes Weight, and back. History has 7D / 30D / 3M / 1Y, the inspected date's value and change, exact-date scrubbing and stepping, and the range's average, lowest and highest. Readings are dots; 3M and 1Y draw weekly averages through them. The 90 available demonstration records occupy their real dates within the year; earlier history is visibly empty. The header frosts only once content scrolls beneath it.
- Normal workout and full-screen music artwork share one reversible progress path. Music buttons stay 48 × 48 px, their glyphs retain their size, and workout buttons stay 56 px high with unchanged semantic colours. Controls translate without scaling. Fixed artwork masks crossfade at shared geometry; the clock has no backing box. Titles remain clipped to their own available region, away from controls.
- Focus keeps touch-responsive dot numbers over full-width actual album artwork. Current metadata and artwork changes reach the page through native events, with polling retained for progress/fallback. Artwork decoding runs off the page thread and new decoded covers crossfade. The plain workout view keeps the same player as a small row above Pause/Finish; its cover opens the artwork view, swipe-down returns, and the timer button provides the keyboard alternative.
- Workout history shows a calendar week with previous/next navigation, session and active-time totals, and seven selectable days. Only that day's summary cards appear; each opens the full saved record, and Back preserves the selected week/day. New completed sessions select their own start day. Time-only sessions retain their recorded time breakdown and explain unavailable estimates.
- Workouts opens with "Start a session": one surface of four rows (Walking, Running, Cycling, Strength), each showing that activity's last recorded session. Every setup shares one layout: the header names the activity, then one panel with the Open workout / Time target segments, a duration field when timed, a GPS switch for outdoor activities only, and an optional weight field; helper copy, Start and Music access rest at the bottom of the screen. The target segments answer a press, a held drag across the labels and the release over either one; a cancelled gesture or a release away from the track restores the previous choice, and a vertical gesture still scrolls. Walking, Running, Cycling and Strength retain setup, optional targets, countdown, pause/resume/finish, saved history and grouped details. Android workout notification actions share the saved session state. Sleep retains stage/interval selection and its labelled chronology; noneditable app text does not open copy/translate controls.

Full motion remains the default. The user can pause the orb or explicitly enable the persistent Reduce motion option. Preserve hidden/offscreen cleanup. Keep the charcoal/silver launcher icon, flat page shortcuts, compact Oxygen overview and flat shaded Body chart.

## Data and music boundaries

Health measurements remain labelled demonstration data ending 8 September 2025. App-started workouts are actual local records. Optional phone GPS records route, distance, speed and available elevation; optional user-entered workout weight supplies a labelled MET energy estimate. Demo body weight is never used for actual workout energy. Watch/native Health data integration and physical outdoor accuracy remain separate work. `C:/HA/HEALTH APP` was not modified.

Music uses the phone's real shared media session after the user enables access. No track, permission failure or errors hide the player; a real paused track remains visible. Missing artwork retains supported metadata/controls. Current artwork uses the best shared bitmap or accessible local content source, bounded at 2048 px/JPEG 94 without enlargement or remote downloads. When a track changes, the previous cover stays on screen until the new cover is ready; only if no cover arrives within two seconds does it fade out.

The cache can warm up to three upcoming covers when the provider exposes accessible local queue artwork. It keeps at most four encoded covers and discards stale work. The checked BitChord queue exposed no eligible upcoming local covers, so zero upcoming covers were cached; audio buffering remains the music app's responsibility. The read-only phone check measured a 1 ms initial bridge read, a later artwork-ready event and a 544 × 544 current cover. Those earlier read-only checks are historical. The 12 September isolated phone run checked real play/pause acknowledgement and notification-access recovery under explicit permission.

## Verification and recovery

`verification/blob-math.cjs` runs the selector mathematics against the shipped `blob-track.js`. `verification/frosted-system.html` drives a real pointer, holds the indicator halfway between two options without releasing and checks the material, the influence actually rendered, the hold cap, re-grab, stillness, cancellation and cleanup. Existing Node logic checks and native artwork/altitude/token/distance checks pass. Rendered UX, focus/music and workout-detail flows pass at 390, 384 and 320 px. They now also cover the target selector's pointer gestures, agreement between the music transition's held and settled ends through reversals, title and transport separation at several progress values, time-only session content, in-progress session controls, weekly-day inspection and the history's naming, grouping and start times. `verification/check-live-background.cjs` checks stable blur dimensions, timer-only DOM changes through repeated state refreshes and unchanged background pixels during reversals. Browser checks use isolated contexts without user workout data.

Build: `python android/build.py`. Preserve `android/signing/orbit-local.p12` for compatible updates. Scripts, CSS and font are bundled offline; compilation, v3 signing, bundled-source checks and the installed APK hash passed. The build saves the previous APK.

Deck fold, live-bar growth, orb drawing and Home renders were profiled and made compositor-friendly on 11 September without changing their look; see the performance section in `HANDOFF.md`. Run `node verification/check.cjs`, `node verification/workout-details.cjs` and `verification/native-check.ps1`. Rendered checks use the installed Playwright package on `NODE_PATH` and the local server at port 8784: `node verification/run-rendered-checks.cjs ux-motion focus-music workout-details frosted-system`; the focused background check also uses installed sharp.

The 12 September phone suite checked twenty deck/Intake reversals, navigation, music acknowledgement, native pause/resume in three views, screen-off elapsed time, one-record saving, permission recovery and larger system text/display size. Browser suites cover generated difficult artwork, delayed media acknowledgements, accessibility semantics and long-history return. Native frame timing remains mixed; audible output, forced provider-network conditions and spoken TalkBack remain unqualified. See the complete per-row results rather than treating installation as visual acceptance.

Recovery: `verification/before-seven-ux-fixes-20260910-183639`, `verification/before-live-bar-review-20260910-191140`, and `verification/before-live-background-20260910-193826`, with checked source backups. Earlier evidence remains historical. The reconnect heartbeat was not changed.
